NODE_ENV=development
PORT=3000
CORS_ORIGIN=http://localhost:4200

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

JWT_SECRET=
JWT_EXPIRES_IN=1h

# Opcional: solo necesario para el envío de correos de "Recordar contraseña"
# (API key de https://resend.com — Dashboard > API Keys)
RESEND_API_KEY=
# Opcional: remitente verificado en Resend. Si se deja vacío, se usa el
# remitente de pruebas onboarding@resend.dev (no requiere dominio verificado).
RESEND_FROM=
