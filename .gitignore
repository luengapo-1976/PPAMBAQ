.mcp.json
.claude/settings.local.json

# Variables de entorno (nunca commitear secretos)
.env
.env.local
.env.*.local

# Dependencias y build (backend NestJS / frontend Angular)
node_modules/
dist/
coverage/
.angular/

# Archivos temporales de Office / sistema
~$*
.DS_Store
Thumbs.db

# Muestras con datos personales reales (no deben subirse a un repo público)
frontend/src/assests/adjuntos/S-73_S Amparo Maida Editado.pdf
